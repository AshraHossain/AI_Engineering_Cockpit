# Copy this file to .env if you want to change the log level. Never commit .env.
#
# This project makes no model calls at all -- the whole demo is governance
# controls over an in-memory registry -- so there is no API key to set. Both
# `uv run python src/main.py --dry-run` and the tests run offline.

LOG_LEVEL=INFO
