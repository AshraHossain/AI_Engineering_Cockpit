[project]
name = "governed-deployment"
version = "0.1.0"
description = "A model deployment pipeline you cannot shortcut: registry, approvals, RBAC, audit trail."
authors = [{ name = "Ash", email = "ashrafuzzmanhossain@gmail.com" }]
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
# "../.." is the repo root. It is `package = false` by design, so it is never
# installed into this project's venv -- putting it on the path is what lets
# these tests import the `cockpit.*` governance framework.
pythonpath = ["src", "../.."]
testpaths = ["tests"]

[tool.ruff.lint.per-file-ignores]
# src/main.py prints the CLI's actual output (the deployment transcript, not a
# diagnostic) -- routing that through logging would prefix every line with
# timestamps and levels. E402: the sys.path bootstrap for the repo root has
# to run before the `cockpit.*` imports it enables.
"src/main.py" = ["T201", "E402"]
