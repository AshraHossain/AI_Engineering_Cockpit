# Copy this file to .env and fill in your key. Never commit .env.
#
# Not needed for `--dry-run`, which runs entirely against a canned offline
# client and makes no network calls.

GEMINI_API_KEY=
LOG_LEVEL=INFO
