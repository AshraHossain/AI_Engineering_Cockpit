# Copy this file to .env if you want to change the log level. Never commit .env.
#
# There is deliberately no API key here. Every control in this project runs
# locally against data/records.json -- no model call, no network, no provider.
# The CLI and the tests both run offline with no configuration at all.

LOG_LEVEL=WARNING
