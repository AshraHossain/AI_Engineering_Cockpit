"""Testing framework: fixtures, example unit/integration tests, and benchmarks.

Enabled by default (see ``cockpit.config.feature_flags``). This package
demonstrates *how* to test cockpit code and downstream projects — the
example tests here are illustrative, not an exhaustive suite.
"""
